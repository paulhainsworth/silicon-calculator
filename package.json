{
  "name": "silicon-calculator",
  "version": "1.0.0",
  "description": "GPU Financing Calculator",
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p public && cp index.html public/ && cp gpu_financing_calculator_4.html public/ || true"
  },
  "keywords": ["calculator", "gpu", "financing"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@vercel/kv": "^0.2.1"
  }
}

